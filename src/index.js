import React from 'react';
import ReactDOM from 'react-dom/client';
import App from './App';

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
  <React.StrictMode>
    <App />
  </React.StrictMode>
);


//Planning to adding testing to this project at some point down the line
//Final project focus for now is getting the site functional
//Ironically this means testing will come later :)